# Build
build/
out/
classes/
src/generated/

# Gradle
.gradle/
!gradle/wrapper/gradle-wrapper.jar

# NeoForge / ModDevGradle
run/
runs/
run-data/
repo/

# IDE / agent tooling
.claude/
.idea/
*.iml
*.ipr
*.iws
.vscode/
.classpath
.project
.settings/
eclipse/

# Reference material — Alex's Mobs jar and its extracted contents.
# GPL-3.0 upstream; kept locally for reference only, never redistributed.
alex-mods/

# Python tooling
__pycache__/
*.pyc

# Reference sprites pulled off the Minecraft Wiki while matching vanilla behaviour, plus a stray
# editor backup. None are used by sync_assets.py, and wiki media is CC BY-NC-SA — not compatible
# with GPL-3.0 and not ours to redistribute. They stay local.
# Matched at any depth. These were written for a flat textures/ folder and silently stopped
# matching once the art was organised into per-pet subfolders, which published them.
textures/**/BlockSprite_*.png
textures/**/Invicon_Sniffer_Egg.webp
textures/**/Ragdoll_Cat_*.webp
textures/**/*.zip
textures/**/ray_bird_shiny.png
textures/BlockSprite_*.png
textures/Invicon_Sniffer_Egg.webp
textures/*.zip
# Alex's Mobs' unused "shiny" variant, kept only for reference.
textures/ray_bird_shiny.png

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

# Mascot voting site (site/)
site/node_modules/
site/.next/
site/out/
site/data/
site/.env*.local
site/next-env.d.ts
site/tsconfig.tsbuildinfo
